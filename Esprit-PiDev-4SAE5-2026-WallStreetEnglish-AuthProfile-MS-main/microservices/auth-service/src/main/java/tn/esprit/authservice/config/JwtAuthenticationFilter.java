package tn.esprit.authservice.config;

/**
 * JWT authentication filter placeholder.
 * Authentication is handled by Keycloak OAuth2 resource server configuration in SecurityConfig.
 */
public class JwtAuthenticationFilter {
    // This class is intentionally empty.
    // JWT validation is delegated to Spring Security's OAuth2 resource server (Keycloak).
}
